/**
 * Represents a set of coordinates within the graph.
 */
export default class Point {

    //{Number} xCoord
    //{Number} yCoord

    /**
     * Creates a point at the given coordinates.
     *
     * @param {Number} xCoord
     * @param {Number} yCoord
     */
    constructor(xCoord, yCoord) {
        this.xCoord = xCoord;
        this.yCoord = yCoord;
    }

}